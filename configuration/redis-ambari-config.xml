<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>

<configuration>

  <!-- Define configuration paramaters for service: property name, default value and description (shown as help text) -->

  <!-- service log file -->
	<property>
		<name>redis.install_dir</name>
		<value>/opt</value>
		<description>Local dir where to install component. Redis folder will be created as a subdir of this dir e.g. /opt/redis-3.0.0</description>
	</property>

	<property>
		<name>redis.port</name>
		<value>6379</value>
		<description>Port Redis runs on</description>
	</property>

  	<property>
    		<name>redis.log</name>
    		<value>/var/log/redis/redis_setup.log</value>
    		<description>Log file for redis service commands used by amabari. Not the Redis server logs</description>
  	</property> 

</configuration>
